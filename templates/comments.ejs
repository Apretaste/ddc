<div class="col s12">
    <ul class="collection" id="comments">
        <% comments.forEach(function (comment) { %>
            <li class="collection-item avatar">
                <span style="background-color: <%= getRandomColor() %>"
                      class="circle"> <%= comment.username[0].toUpperCase() %> </span>
                <span class="title" style="color: #303d44; font-weight: 500">@<%= comment.username %> &middot; <small
                            class="grey-text text-darken-2"><b><%= comment.inserted %></b></small></span>
                <p><%= comment.content %></p>

                <% if(comment.id_article) {%>
                    <div class="card grey-text text-darken-2" onclick="apretaste.send({'command': 'DDC HISTORIA', 'data':{'id': '<%= comment.id_article %>'}})">
                        <span style="color: #303d44;"><%= comment.title %></span><br>
                        <small>
                            <i class="material-icons tiny">calendar_today</i> <%= comment.pubDate %> &nbsp;
                            <i class="material-icons tiny">person</i> <%= comment.author %>
                        </small>
                    </div>
                <%}%>
            </li>
        <% }) %>
    </ul>
</div>

<div id="writeModal" class="container" status="closed" style="display: none;">
    <div class="row">
        <i class="material-icons small" onclick="toggleWriteModal()">close</i>
        <div class="col s12">
            <textarea id="comment" class="materialize-textarea" maxlength="250"
                      placeholder="¿En qué piensas?"></textarea>
        </div>

        <div class="actions">
            <i class="material-icons small right" onclick="sendComment()">send</i>
        </div>
    </div>
</div>

<div class="fixed-action-btn">
    <a class="btn-floating btn-large grey darken-2" onclick="toggleWriteModal()">
        <i class="large material-icons">comment</i>
    </a>
</div>
